name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build and release (must already exist), e.g. v0.2.0 or v0.2.0-nightly"
        required: true

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: read

jobs:
  create_draft_release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      tag: ${{ steps.resolve.outputs.tag }}
      version: ${{ steps.version.outputs.version }}
      channel: ${{ steps.channel.outputs.channel }}
    steps:
      - id: resolve
        name: Resolve tag
        run: echo "tag=${{ inputs.tag || github.ref_name }}" >> "$GITHUB_OUTPUT"
      - id: version
        name: Compute plain version (no leading v)
        run: echo "version=$(echo '${{ steps.resolve.outputs.tag }}' | sed 's/^v//')" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: ${{ steps.resolve.outputs.tag }}
      - id: channel
        name: Determine release channel
        run: |
          if [[ "${{ steps.resolve.outputs.tag }}" == *-nightly ]]; then
            echo "channel=nightly" >> "$GITHUB_OUTPUT"
          else
            echo "channel=stable" >> "$GITHUB_OUTPUT"
          fi
      - name: Generate release notes
        run: bash script/draft-release-notes.sh "${{ steps.resolve.outputs.tag }}" > release-notes.md
      - name: Create draft release
        run: bash script/create-draft-release.sh release-notes.md ${{ steps.channel.outputs.channel }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REF_NAME: ${{ steps.resolve.outputs.tag }}

  bundle:
    needs: create_draft_release
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            arch: x86_64
            target: x86_64-pc-windows-msvc
          - os: windows-11-arm
            arch: aarch64
            target: aarch64-pc-windows-msvc
          - os: ubuntu-latest
            arch: x86_64
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            arch: aarch64
            target: aarch64-unknown-linux-gnu
          - os: macos-latest
            arch: aarch64
            target: aarch64-apple-darwin
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.create_draft_release.outputs.tag }}
      - uses: dtolnay/rust-toolchain@nightly
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: always()

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev \
            libssl-dev \
            pkg-config \
            libgtk-3-dev \
            libxdo-dev \
            libayatana-appindicator3-dev

      - name: Bundle (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./script/bundle-windows.ps1 -Architecture ${{ matrix.arch }} -Version ${{ needs.create_draft_release.outputs.version }} -Channel ${{ needs.create_draft_release.outputs.channel }}

      - name: Bundle (macOS)
        if: runner.os == 'macOS'
        run: bash script/bundle-mac.sh ${{ matrix.arch }} ${{ needs.create_draft_release.outputs.version }} ${{ needs.create_draft_release.outputs.channel }}

      - name: Bundle (Linux)
        if: runner.os == 'Linux'
        run: bash script/bundle-linux.sh ${{ matrix.arch }} ${{ needs.create_draft_release.outputs.version }} ${{ needs.create_draft_release.outputs.channel }}

      - name: Upload artifact
          uses: actions/upload-artifact@v7
        with:
          name: stealcode-${{ runner.os }}-${{ matrix.arch }}
          path: |
            target/StealCode-*.exe
            target/StealCode-*.dmg
            target/stealcode-linux-*.tar.gz
          if-no-files-found: error

  upload_release_assets:
    needs:
      - create_draft_release
      - bundle
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true
      - name: Upload to release
        run: gh release upload "${{ needs.create_draft_release.outputs.tag }}" --repo he-thinks/stealcode artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  validate_release_assets:
    needs:
      - create_draft_release
      - upload_release_assets
    runs-on: ubuntu-latest
    steps:
      - name: Validate expected assets are present
        run: |
          EXPECTED='["StealCode-x86_64.exe", "StealCode-aarch64.exe", "StealCode-x86_64.dmg", "StealCode-aarch64.dmg", "stealcode-linux-x86_64.tar.gz", "stealcode-linux-aarch64.tar.gz"]'
          ACTUAL=$(gh release view "${{ needs.create_draft_release.outputs.tag }}" --repo he-thinks/stealcode --json assets -q '[.assets[].name]')
          MISSING=$(echo "$EXPECTED" | jq -r --argjson actual "$ACTUAL" '. - $actual | .[]')
          if [ -n "$MISSING" ]; then
            echo "Missing assets:"
            echo "$MISSING"
            exit 1
          fi
          echo "All expected assets present."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
